# PyTorch is intentionally omitted: use the CUDA build supplied by Kaggle or
# install the platform-appropriate build separately.
numpy>=1.24
Pillow>=9.5
pytest>=7.0
huggingface-hub>=0.20
matplotlib>=3.7
